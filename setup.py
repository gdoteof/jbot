from setuptools import setup

setup(name='jbot',
        version="0.0.1",
        description=f"jeremy howard's bag of tricks",
        url='http://fast.ai',
        author='jeremy howard, curated by geoff golder',
        license='mit',
        packages=['jbot'],
        zip_safe=True)
